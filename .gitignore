# ----------------------------------------
# 1. Віртуальне Середовище (CRITICAL)
# ----------------------------------------
/venv/
# Або будь-яка інша папка віртуального середовища, якщо не venv
# /env/

# ----------------------------------------
# 2. Локальні Кеші та Специфічні для Python/IDE Файли
# ----------------------------------------
__pycache__/
*.pyc

# Кеші PySide/Qt та IDE (наприклад, VSCode)
.vscode/
.qt/

# ----------------------------------------
# 3. Вихідні Дані Проєкту та Великі Файли (CRITICAL)
# ----------------------------------------
# Результати обробки та самаризації (їх не потрібно пушити)
/extracted_documents/
/output_summaries/

# ----------------------------------------
# 4. Файли Збірки PyInstaller та Кросплатформні Бінарники
# ----------------------------------------
# Результати збірки та тимчасові файли
/build/
/dist/
*.spec # Якщо ви використовуєте його для збірки на CI/CD, його можна залишати, але якщо це тільки тимчасовий файл, краще ігнорувати.
# Оскільки вам потрібна конфігурація PyInstaller для CI/CD, ЗАЛИШТЕ forensic_summarizer.spec НЕ ЗАКОМЕНТОВАНИМ.

# ----------------------------------------
# 5. Git LFS (Хоча файл моделі відстежується LFS,
#    додаткові великі бінарники можна ігнорувати)
# ----------------------------------------
*backend/llm_models/mistral-7b-instruct-v0.1.Q4_K_M.gguf
# Ми відстежуємо цей файл через LFS, тому ми його НЕ ІГНОРУЄМО.

# ----------------------------------------
# 6. Специфічні для Windows Файли
# ----------------------------------------
Thumbs.db